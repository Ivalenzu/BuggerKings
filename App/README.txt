Módulos importantes a importar en Python

pandas: pip install pandas
scipy : pip install scipy
numpy : pip install numpy
tkinter: Depende de la distribución ya que no se instala mediante pip, si no por el gestor de paquetes.